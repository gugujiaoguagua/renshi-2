# 基本信息

- 任务编号：TASK-20260528-003
- 任务名称：后端整体主线整理
- 创建日期：2026-05-28
- 完成日期：2026-05-28
- 负责人：Codex
- 状态：已完成

## 用户原始请求

```text
开始，并且最终一定是，先有组织——再有员工——然后考勤——最后薪酬
```

## 任务目标

把 `D:\我的应用\打卡前+后\后端` 按统一主线整理，而不是拆成零散碎片。最终后端业务顺序固定为：组织 -> 员工 -> 考勤 -> 薪酬。

## 执行边界

- 不为了拆分而拆分。
- 不复刻 `新后端` 的 UI。
- 不改前端页面、菜单、路由。
- 当前阶段先统一后端主线、依赖关系、路由区块说明和可验证输出。

## 完成内容

1. 把零散 core 小文件归并为一个 `server/core/foundation.mjs`，避免底层能力七零八落。
2. 在 `foundation.mjs` 中固定后端业务域顺序和依赖关系：organization -> employee -> attendance -> payroll。
3. `/api/health` 输出 `domainFlow`，作为当前后端运行时可核对的统一主线。
4. `/api/hr-core/migration-bundle` 输出同一份 `domainFlow`，并按组织、员工、考勤、薪酬调整导入顺序。
5. 在 `server/data-server.mjs` 路由区块上标注统一顺序，后续整理按这个顺序继续推进。

## 输出物

| 输出物 | 路径 | 状态 |
|---|---|---|
| 后端基础层 | `server/core/foundation.mjs` | 已完成 |
| 基础层说明 | `server/core/README.md` | 已完成 |
| 后端主服务接入 | `server/data-server.mjs` | 已完成 |

## 验收结果

| 验收项 | 结果 |
|---|---|
| `node --check server/data-server.mjs` | 通过 |
| `node --check server/core/foundation.mjs` | 通过 |
| `npm run build` | 通过，有既有 Vite chunk/xlsx 警告 |
| 临时端口 `/api/health` | 通过，`domainFlow = organization -> employee -> attendance -> payroll` |
| 临时端口 `/api/hr-core/migration-bundle` | 通过，`domainFlow = organization -> employee -> attendance -> payroll` |
| 迁移导入顺序 | 前四项为组织三项 + 员工，最后一项为 `statItems` |

## 下一步

下一步不继续拆小文件，建议先整理“组织”这一块的底层逻辑：确认组织结构、岗位、职级、组织设置四类数据的来源、覆盖关系、写入规则和给员工模块的输出口径。
