# 基本信息

- 任务编号：TASK-20260528-005
- 任务名称：员工底层逻辑整理
- 创建日期：2026-05-28
- 完成日期：2026-05-28
- 负责人：Codex
- 状态：已完成

## 用户原始请求

```text
继续
```

## 执行主线

本次继续按固定顺序推进：组织 -> 员工 -> 考勤 -> 薪酬。

## 任务目标

只整理“员工”底层逻辑，把员工主数据来源、组织/岗位/职级关联规则、写入规则、输出给考勤和薪酬的口径统一起来。

## 执行边界

- 不碰 UI。
- 不继续拆散文件。
- 不进入考勤、薪酬实现。
- 不改变原有员工花名册、档案、任职、合同接口的返回结构。

## 完成内容

1. 新增员工域统一快照 `employeeDomainSnapshot()`。
2. `employeeSummary()` 改为从统一快照取汇总，避免员工统计口径分散。
3. 新增 `/api/employee-management/foundation`，输出员工数据来源、上游组织依赖、输出给考勤和薪酬的契约、质量检查。
4. 将员工管理设置行归到 `employeeSettingsRows()`，避免 settings 路由里散落一份单独定义。
5. 保持原有员工花名册、档案、任职、合同接口返回结构不变。

## 输出物

| 输出物 | 路径 | 状态 |
|---|---|---|
| 员工域统一快照 | `server/data-server.mjs` | 已完成 |
| 员工基础检查接口 | `/api/employee-management/foundation` | 已完成 |
| 员工设置统一函数 | `server/data-server.mjs` | 已完成 |

## 验收结果

| 验收项 | 结果 |
|---|---|
| `node --check server/data-server.mjs` | 通过 |
| `node --check server/core/foundation.mjs` | 通过 |
| `npm run build` | 通过，有既有 Vite chunk/xlsx 警告 |
| `/api/employee-management/foundation` | 通过，`domain = employee` |
| `/api/employee-management/summary` | 通过，员工总数 1035 |
| `/api/employee-management/roster` | 通过，花名册 1035 |
| `/api/employee-management/archive` | 通过，档案 1035 |
| `/api/employee-management/employment/onboarded` | 通过，已入职 103 |
| `/api/employee-management/contracts/all` | 通过，合同 1002 |
| 员工质量检查 | 重复员工号 0，组织未关联 1，岗位未关联 22 |

## 下一步

下一步进入“考勤”底层逻辑整理：考勤必须以员工号为人员识别主键，消费员工域输出的部门、岗位、考勤组、班次，再输出月汇总给薪酬。
