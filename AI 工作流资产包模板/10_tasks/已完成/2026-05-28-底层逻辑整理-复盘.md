# 复盘：底层逻辑整理

## 结论

本次只参考 `D:\我的应用\打卡前+后\新后端` 的底层分层方式，没有复刻任何 UI 项。当前后端先完成了 core 层第一步整理：运行配置和 JSON Store 从 `server/data-server.mjs` 中归入统一基础层。

## 做对的地方

- 先整理底层公共能力，没有碰前端页面、菜单或路由。
- 保留了 `data-store.json` 原有缓存语义和写入后的 HR 核心缓存失效行为。
- 用临时端口启动新代码做了真实 API smoke test，避免只停留在构建通过。
- 根据“不要七零八落”的反馈，底层文件已归并为 `server/core/foundation.mjs`。

## 风险和注意

- `server/data-server.mjs` 仍然很大，业务服务、路由、规则还没有拆分。
- 后续拆 feature 时要继续保持 API 返回结构不变。
- `npm run build` 仍有 Vite 大包和 `xlsx` 分包警告，本次未处理，因为不属于底层逻辑整理范围。

## 下一步

建议下一张任务卡只拆一个后端业务域，例如先从组织数据读取服务开始，将 Excel/参考文件读取和组织汇总逻辑移入 `server/features/organization`，不处理任何 UI。
