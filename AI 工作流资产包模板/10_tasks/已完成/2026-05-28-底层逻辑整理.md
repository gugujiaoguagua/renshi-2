# 基本信息

- 任务编号：TASK-20260528-002
- 任务名称：只参考新后端整理底层逻辑
- 创建日期：2026-05-28
- 完成日期：2026-05-28
- 负责人：Codex
- 状态：已完成

## 用户原始请求

```text
选择开始整理底层逻辑，【"D:\我的应用\打卡前+后\新后端"只看底层逻辑，不要复刻任何ui项】
```

## 任务目标

参考 `D:\我的应用\打卡前+后\新后端` 的后端分层思路，先整理当前后端的底层配置和本地数据仓库能力，保持现有 API 行为不变，不复制任何 UI 路由、菜单或页面。

## 输入材料

| 材料 | 路径 | 用途 | 是否齐全 |
|---|---|---|---|
| 新后端底层逻辑 | `D:\我的应用\打卡前+后\新后端\src\core` | 参考配置、数据库、HTTP 基础层边界 | 是 |
| 新后端业务模块 | `D:\我的应用\打卡前+后\新后端\src\features` | 参考后端业务分层，不参考 UI | 是 |
| 当前后端 | `server/data-server.mjs` | 待整理的单体服务 | 是 |

## 执行边界

- 只整理底层逻辑。
- 不复刻 `新后端` 的任何 UI 项。
- 不改当前前端页面、菜单、路由。
- 不改变接口返回结构。

## 完成内容

1. 新增 `server/core/README.md`，明确底层逻辑边界，禁止 UI 路由、菜单、页面定义进入 core。
2. 新增后又按整体归拢原则合并为 `server/core/foundation.mjs`，集中管理端口、主机、环境变量、数据目录路径和 JSON Store。
3. `server/data-server.mjs` 改为从 core 基础层导入底层能力，业务路由和返回结构保持不变。
4. 用 `onStoreWrite` 监听数据写入，保留原有写入后清理 `hrCoreCache` 的行为。

## 输出物

| 输出物 | 路径 | 状态 |
|---|---|---|
| 后端基础层模块 | `server/core/foundation.mjs` | 已完成 |
| 底层边界说明 | `server/core/README.md` | 已完成 |
| 服务接入修改 | `server/data-server.mjs` | 已完成 |

## 验收结果

| 验收项 | 结果 |
|---|---|
| `node --check server/data-server.mjs` | 通过 |
| `node --check server/core/foundation.mjs` | 通过 |
| `npm run build` | 通过，有既有 Vite chunk/xlsx 警告 |
| 临时端口 `/api/health` | 通过，`ok: true` |
| 临时端口 `/api/organization-management/summary` | 通过，组织 199、职位 367、职级 15 |

## 后续建议

- 下一步可以继续按同样方式拆业务层：组织、员工、考勤、薪资分别进入 feature/service 模块。
- 每次只拆一个后端能力，避免把 UI 项或菜单结构带进底层逻辑。
