# 基本信息

- 日期：2026-05-28
- 任务编号：TASK-20260528-001
- 任务名称：清理不要的界面并按 AI 友好代码规范整理
- 使用 skill：Code、app-builder-agent
- 是否更新资产包：是

## 本次完成

- 新增任务卡并按完成状态归档。
- 新增 `src/app/shared/navigation/README.md`，说明导航可见性模块边界。
- 新增 `src/app/shared/navigation/visibilityPolicy.ts`，集中管理被移除界面、员工视图、组织 section、考勤直达路由重定向。
- 将路由、组织页、员工页、考勤设置页接入统一规则。
- 完成 `npm run build` 验证。

## 有效流程

- 先读取资产包使用说明、规则总表、AI 友好代码架构规范和质量检查清单。
- 先补任务卡和模块 README，再抽离代码规则。
- 对“删除界面”采用隐藏入口和直达重定向，不直接删除页面实现文件，降低误删风险。

## 新增业务规则

| 规则 | 来源 | 建议去向 |
|---|---|---|
| 被清理的界面应统一登记到 `src/app/shared/navigation/visibilityPolicy.ts`，不要散落在各页面里重复判断。 | 本次代码整理 | memory / 规则候选 |
| 未明确要求删除源码时，只移除入口和直达路径，保留页面实现文件。 | 用户强调“只删除这些不要的，其他不要乱动” | memory |

## AI 误判或不确定项

- “清理界面”在当前阶段按入口清理和直达 URL 重定向处理，不做源码物理删除。
- 源码中保留的不可达组件仍包含旧文案，属于保守保留，不代表界面入口仍存在。

## 用户认可的输出格式

- 简短说明已完成范围、验证结果和保留风险。

## 可复用资产

- skill 候选：后台可视化界面裁剪与导航可见性整理。
- 模板候选：界面清理任务卡。
- 工具候选：无。
- 应用化候选：无新增。

## 下次同类任务建议

- 优先读取 `src/app/shared/navigation/README.md` 和 `visibilityPolicy.ts`。
- 如果用户继续说“清理界面”，先改统一规则，再检查菜单、首页快捷入口和直达路由。
- 如果用户明确要求“删除源码”，再按模块单独评估页面文件、API 引用和构建影响。
