# 基本信息

- 任务编号：TASK-20260528-004
- 任务名称：组织底层逻辑整理
- 创建日期：2026-05-28
- 完成日期：2026-05-28
- 负责人：Codex
- 状态：已完成

## 用户原始请求

```text
继续
```

## 执行主线

本次继续按固定顺序推进：组织 -> 员工 -> 考勤 -> 薪酬。

## 任务目标

只整理“组织”底层逻辑，先把组织结构、岗位、职级、组织设置四类数据的来源、覆盖关系、写入规则、输出给员工模块的口径统一起来。

## 执行边界

- 不碰 UI。
- 不继续拆散文件。
- 不进入员工、考勤、薪酬实现。
- 不改变原有组织列表、岗位列表、职级列表接口的返回结构。

## 完成内容

1. 新增组织域统一快照 `organizationDomainSnapshot()`。
2. `organizationSummary()` 改为从统一快照取汇总，避免组织统计口径分散。
3. 新增 `/api/organization-management/foundation`，输出组织域数据来源、读取规则、写入规则、输出给员工模块的契约和质量检查。
4. 组织基础检查覆盖重复组织编码、重复组织全路径、孤儿组织、未关联岗位、未关联职级。
5. 原有组织、岗位、职级、设置接口保持原返回结构。

## 输出物

| 输出物 | 路径 | 状态 |
|---|---|---|
| 组织域统一快照 | `server/data-server.mjs` | 已完成 |
| 组织基础检查接口 | `/api/organization-management/foundation` | 已完成 |
| 后端域查询 helper | `server/core/foundation.mjs` | 已完成 |

## 验收结果

| 验收项 | 结果 |
|---|---|
| `node --check server/data-server.mjs` | 通过 |
| `node --check server/core/foundation.mjs` | 通过 |
| `npm run build` | 通过，有既有 Vite chunk/xlsx 警告 |
| `/api/organization-management/foundation` | 通过，`domain = organization` |
| `/api/organization-management/summary` | 通过，组织总数 199 |
| `/api/organization-management/organizations` | 通过，组织总数 199 |
| `/api/organization-management/positions` | 通过，岗位总数 367 |
| `/api/organization-management/ranks` | 通过，职级总数 15 |
| 组织质量检查 | 重复组织编码 0，孤儿组织 0 |

## 下一步

下一步进入“员工”底层逻辑整理：员工必须消费组织域输出的 `fullPath/name`、岗位 `name`、职级 `code/name`，再向考勤人员、排班和薪酬统计输出统一员工主数据。
