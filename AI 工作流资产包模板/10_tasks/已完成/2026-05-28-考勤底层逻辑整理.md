# 基本信息

- 任务编号：TASK-20260528-006
- 任务名称：考勤底层逻辑整理
- 创建日期：2026-05-28
- 完成日期：2026-05-28
- 负责人：Codex
- 状态：已完成

## 用户原始请求

```text
开始
```

## 执行主线

本次继续按固定顺序推进：组织 -> 员工 -> 考勤 -> 薪酬。

## 任务目标

只整理“考勤”底层逻辑，把考勤人员、考勤设置、排班、打卡、日/月统计、假勤外勤和输出给薪酬的口径统一起来。

## 执行边界

- 不碰 UI。
- 不继续拆散文件。
- 不进入薪酬实现。
- 不改变原有考勤接口返回结构。

## 完成内容

1. 新增考勤域统一快照 `attendanceDomainSnapshot()`。
2. 固定考勤域人员识别规则：员工号是唯一人员主键，姓名、部门、岗位、考勤组、班次只作为计算和展示口径。
3. 新增 `/api/attendance-management/foundation`，输出考勤人员、设置、排班、打卡、日/月统计、假勤外勤的数据来源、写入规则、输出给薪酬的契约。
4. 增加考勤质量检查：存储排班、打卡、日报、月汇总、假勤、外勤、外部数据是否能按员工号关联到员工域。
5. 保持原有考勤统计、日报、月报、月汇总、排班、打卡、设置接口返回结构不变。

## 输出物

| 输出物 | 路径 | 状态 |
|---|---|---|
| 考勤域统一快照 | `server/data-server.mjs` | 已完成 |
| 考勤基础检查接口 | `/api/attendance-management/foundation` | 已完成 |
| 考勤输出给薪酬契约 | `server/data-server.mjs` | 已完成 |

## 验收结果

| 验收项 | 结果 |
|---|---|
| `node --check server/data-server.mjs` | 通过 |
| `node --check server/core/foundation.mjs` | 通过 |
| `npm run build` | 通过，有既有 Vite chunk/xlsx 警告 |
| `/api/attendance-management/foundation` | 通过，`domain = attendance` |
| `/api/attendance-stats` | 通过，1035 |
| `/api/daily-attendance` | 通过，1035 |
| `/api/monthly-attendance` | 通过，1035 |
| `/api/monthly-summary` | 通过，1035 |
| `/api/clock-records` | 通过，6 |
| `/api/schedules/month` | 通过，1035 |
| `/api/settings-groups` | 通过，7 |
| `/api/settings-shifts` | 通过，25 |
| 考勤质量检查 | 存储排班未关联 0，存储打卡未关联 0 |

## 下一步

下一步进入“薪酬”底层逻辑整理：薪酬只能消费员工主数据、考勤月汇总和外部统计项，不应绕过考勤域直接读原始打卡明细。
