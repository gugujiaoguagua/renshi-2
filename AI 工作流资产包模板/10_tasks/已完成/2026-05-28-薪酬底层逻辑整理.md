# 基本信息

- 任务编号：TASK-20260528-007
- 任务名称：薪酬底层逻辑整理
- 创建日期：2026-05-28
- 完成日期：2026-05-28
- 负责人：Codex
- 状态：已完成

## 用户原始请求

```text
继续
```

## 执行主线

本次继续按固定顺序推进：组织 -> 员工 -> 考勤 -> 薪酬。

## 任务目标

只整理“薪酬”底层逻辑，先明确薪酬可消费的数据边界、统计项、外部数据、月汇总取数和后续公式追踪契约。

## 执行边界

- 不碰 UI。
- 不继续拆散文件。
- 不在本轮迁移前端本地薪酬计算。
- 不改变原有统计项和外部数据接口返回结构。

## 完成内容

1. 新增薪酬域统一快照 `payrollDomainSnapshot()`。
2. 固定薪酬域上游边界：员工主数据、考勤月汇总、统计项、外部数据。
3. 新增 `/api/payroll-management/foundation`，输出薪酬数据来源、允许输入、禁止输入、公式追踪结构和质量检查。
4. 固定公式追踪结构为 `expression`、`inputs`、`blockers`、`notes`。
5. 保持原有 `/api/stat-items` 和 `/api/external-records` 返回结构不变。
6. 在 `server/core/foundation.mjs` 中补充薪酬基础接口前缀 `/api/payroll-management`。

## 输出物

| 输出物 | 路径 | 状态 |
|---|---|---|
| 薪酬域统一快照 | `server/data-server.mjs` | 已完成 |
| 薪酬基础检查接口 | `/api/payroll-management/foundation` | 已完成 |
| 薪酬接口前缀 | `server/core/foundation.mjs` | 已完成 |

## 验收结果

| 验收项 | 结果 |
|---|---|
| `node --check server/data-server.mjs` | 通过 |
| `node --check server/core/foundation.mjs` | 通过 |
| `npm run build` | 通过，有既有 Vite chunk/xlsx 警告 |
| `/api/payroll-management/foundation` | 通过，`domain = payroll` |
| `/api/health` | 通过，`organization -> employee -> attendance -> payroll` |
| `/api/employee-management/foundation` | 通过，员工总数 1035 |
| `/api/attendance-management/foundation` | 通过，考勤月汇总 1035 |
| `/api/monthly-summary` | 通过，1035 |
| `/api/stat-items` | 通过，统计项 28 |
| `/api/external-records` | 通过，外部数据 0 |
| 薪酬质量检查 | 月汇总未关联 0，外部数据未关联 0 |
| 公式追踪结构 | `expression, inputs, blockers, notes` |

## 下一步

四段主线已经完成底层口径整理。下一步建议做一次总体验收：统一检查四个 foundation 接口、健康检查、迁移包、原主要业务接口，并整理一份“后端整体底层逻辑说明”。
